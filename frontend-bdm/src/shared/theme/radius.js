/**
 * Fluent 2 Border Radii — CKR Connect BDM Mobile
 * Fluent uses small, subtle, sharp corners
 */
export const radius = {
  xs: 2,
  sm: 4,
  md: 8,
  lg: 12,
  pill: 9999,
};

export default radius;
